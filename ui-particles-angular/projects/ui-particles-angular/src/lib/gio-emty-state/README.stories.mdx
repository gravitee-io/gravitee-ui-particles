import { Meta, Story } from '@storybook/addon-docs';

<Meta title="Components / Empty state / README" />

# Empty stats

Documentation and illustrations for the empty state component, which is utilized to present a message when there is no content to show on the page.

## Demo

<Story id="components-empty-state--default"></Story>

```html
<gio-empty-state [icon]="search" title="No API found" subtitle="No API found. Please try again or create one."></gio-empty-state>
```
